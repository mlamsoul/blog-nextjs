{
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "buildCommand": "git config --global url.'https://$GIT_SUBMODULE_TOKEN@github.com/'.insteadOf 'https://github.com/' && git submodule update --init --remote && node scripts/sync-images.mjs && next build"
}
